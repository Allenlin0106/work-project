<UserControl x:Class="WorkProject.UI.Views.ProjectGanttView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:gantt="clr-namespace:WorkProject.UI.Controls.Gantt">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Zoom" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <Slider Minimum="8" Maximum="60" Value="{Binding PixelsPerDay}" Width="180" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding PixelsPerDay, StringFormat=0.0}" Margin="8,0,0,0" VerticalAlignment="Center"/>
        </DockPanel>
        <gantt:GanttView Grid.Row="1"/>
    </Grid>
</UserControl>
