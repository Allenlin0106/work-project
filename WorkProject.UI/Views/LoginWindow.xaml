<Window x:Class="WorkProject.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WorkProject - Login"
        Width="380" Height="240"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Sign in" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="User name"/>
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Height="26"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="Password"/>
            <PasswordBox x:Name="PasswordBox" Height="26"/>
        </StackPanel>
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,8,0,0" TextWrapping="Wrap"/>
        <Button Grid.Row="4" Content="Sign in" HorizontalAlignment="Right" Width="100" Height="28"
                Style="{StaticResource PrimaryButton}"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=PasswordBox}"
                IsDefault="True"/>
    </Grid>
</Window>
