<UserControl x:Class="WorkProject.UI.Views.ProjectDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:WorkProject.UI.Views">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding Project.Name}" FontSize="22" FontWeight="Bold"/>
            <TextBlock FontSize="12" Foreground="Gray">
                <Run Text="{Binding Project.StartDate, StringFormat=yyyy-MM-dd}"/>
                <Run Text=" - "/>
                <Run Text="{Binding Project.EndDate, StringFormat=yyyy-MM-dd}"/>
                <Run Text=" | Owner: "/>
                <Run Text="{Binding Project.OwnerDisplayName}"/>
            </TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,8">
            <Button Content="Add task" Command="{Binding AddTaskCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,6,0"/>
            <Button Content="Edit task" Command="{Binding EditTaskCommand}" Margin="0,0,6,0"/>
            <Button Content="Delete task" Command="{Binding DeleteTaskCommand}" Margin="0,0,6,0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,6,0"/>
            <Button Content="Export PDF" Command="{Binding ExportPdfCommand}"
                    CommandParameter="{Binding ElementName=GanttHost}" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>

        <TabControl Grid.Row="2">
            <TabItem Header="Tasks">
                <DataGrid ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}"
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Assignee" Binding="{Binding AssignedDisplayName}" Width="120"/>
                        <DataGridTextColumn Header="Start" Binding="{Binding StartDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                        <DataGridTextColumn Header="End" Binding="{Binding EndDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                        <DataGridTextColumn Header="Progress" Binding="{Binding ProgressPercent, StringFormat={}{0}%}" Width="80"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Gantt">
                <views:ProjectGanttView x:Name="GanttHost" DataContext="{Binding GanttViewModel}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
